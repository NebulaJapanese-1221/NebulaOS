[package]
name = "drivers"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lib = { path = "../lib" }
common = { path = "../common" }

[lib]
crate-type = ["staticlib"]
